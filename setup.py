from setuptools import setup, find_packages

setup(
    name="unissono",
    description="tbd",
    author="emperspectiva",
    version="0.1",
    packages=find_packages(),
    test_requires=[
        "pytest",
    ],
)
